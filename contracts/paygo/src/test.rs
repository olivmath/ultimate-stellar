#![cfg(test)]

// Validate company has employees
// Validate if all employee account id are valid
// Validate no duplicate employees

// owner must:
// - have balance enough to cover total cost
// - pay with `USDC` token
